apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: qwen3-4b
  namespace: inference
spec:
  accessModes: [ReadWriteOnce]
  resources:
    requests:
      storage: 50Gi
  storageClassName: gp3
